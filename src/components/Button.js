import React from 'react';
import {TouchableOpacity, Text} from 'react-native';

const Button = props => {
  return (
    <TouchableOpacity>
      <Text>Detail</Text>
    </TouchableOpacity>
  );
};
export default Button;
